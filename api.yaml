# Generated using @payzen/openapi-testkit DO NOT EDIT THIS FILE!
openapi: 3.0.3
info:
  title: "@payzen/fastify-commons"
  version: 4.13.6
  description: ""
components:
  schemas:
    BestReply:
      title: BestReply
      type: object
      properties:
        welcome:
          type: string
      required:
        - welcome
paths:
  /request-id:
    get:
      responses:
        "200":
          description: Default Response
  /log:
    get:
      responses:
        "200":
          description: Default Response
  /log/debug:
    get:
      responses:
        "200":
          description: Default Response
  /log/silent:
    get:
      responses:
        "200":
          description: Default Response
  /log/html:
    get:
      responses:
        "200":
          description: Default Response
  /got:
    get:
      responses:
        "200":
          description: Default Response
  /body:
    post:
      responses:
        "200":
          description: Default Response
  /outgoing-request:
    post:
      responses:
        "200":
          description: Default Response
  /buffer:
    post:
      responses:
        "200":
          description: Default Response
  /stream:
    post:
      responses:
        "200":
          description: Default Response
  /greet/{greeting}:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                age:
                  type: integer
                name:
                  type: string
                nonEmptyString:
                  type: string
                  minLength: 1
                nested:
                  type: object
                  properties:
                    a:
                      type: string
                    nested:
                      type: object
                      properties:
                        a:
                          type: string
                        b:
                          type: string
                          enum:
                            - aba
                            - ganuv
                      required:
                        - a
                        - b
                  required:
                    - a
              required:
                - age
                - name
        required: true
      parameters:
        - schema:
            enum:
              - Mr
              - Ms
              - Mrs
          in: query
          name: title
          required: true
        - schema:
            type: string
          in: path
          name: greeting
          required: true
        - schema:
            type: string
          in: header
          name: exclamation
          required: true
      responses:
        2XX:
          description: Default Response
          content:
            application/json:
              schema:
                title: BestReply
                type: object
                properties:
                  welcome:
                    type: string
                required:
                  - welcome
  /ingress/greet/{greeting}:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                age:
                  type: integer
                name:
                  type: string
                nonEmptyString:
                  type: string
                  minLength: 1
                nested:
                  type: object
                  properties:
                    a:
                      type: string
                    nested:
                      type: object
                      properties:
                        a:
                          type: string
                        b:
                          type: string
                          enum:
                            - aba
                            - ganuv
                      required:
                        - a
                        - b
                  required:
                    - a
              required:
                - age
                - name
        required: true
      parameters:
        - schema:
            enum:
              - Mr
              - Ms
              - Mrs
          in: query
          name: title
          required: true
        - schema:
            type: string
          in: path
          name: greeting
          required: true
        - schema:
            type: string
          in: header
          name: exclamation
          required: true
      responses:
        2XX:
          description: Default Response
          content:
            application/json:
              schema:
                title: BestReply
                type: object
                properties:
                  welcome:
                    type: string
                required:
                  - welcome
  /ingress/session:
    get:
      responses:
        "200":
          description: Default Response
  /error:
    get:
      responses:
        "200":
          description: Default Response
  /error/fatal:
    get:
      responses:
        "200":
          description: Default Response
  /error/unexpected:
    get:
      responses:
        "200":
          description: Default Response
  /ingress/error:
    get:
      responses:
        "200":
          description: Default Response
  /ingress/error/causations:
    get:
      responses:
        "200":
          description: Default Response
  /ingress/error/no-user-message:
    get:
      responses:
        "200":
          description: Default Response
  /ingress/error/only-status-code:
    get:
      responses:
        "200":
          description: Default Response
  /ingress/error/unexpected:
    get:
      responses:
        "200":
          description: Default Response
  /ingress/error/ajverrors:
    get:
      parameters:
        - schema:
            type: string
            pattern: ^d+$
            errorMessage:
              pattern: is invalid id format
          in: query
          name: foo
          required: false
      responses:
        "200":
          description: Default Response
  /ingress/error/user-details:
    get:
      responses:
        "200":
          description: Default Response
  /ingress/error/user-details/error:
    get:
      responses:
        "200":
          description: Default Response
  /ingress/error/error-like:
    post:
      responses:
        "200":
          description: Default Response
  /arrays:
    get:
      parameters:
        - schema:
            type: array
            items:
              type: number
          in: query
          name: random_nums
          required: true
      responses:
        2XX:
          description: Default Response
          content:
            application/json:
              schema:
                type: array
                items:
                  type: number
